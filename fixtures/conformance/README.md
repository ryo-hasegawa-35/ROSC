# ROSC Conformance Fixtures

This directory stores seed fixtures and catalog metadata for future OSC
conformance tests.

Key files:

- `catalog.json`
- `vectors/*.hex.txt`

The fixture files currently act as a pre-implementation seed corpus, not a
finalized automation format.
