# ROSC Benchmark Fixtures

This directory stores benchmark workload inventory and metadata templates for
future performance evidence.

Key files:

- `workload-catalog.json`
- `context-template.json`

These files define reproducible workload intent before benchmark code exists.
