# ROSC Benchmark Fixture

このディレクトリには、将来の performance evidence に使う benchmark workload
inventory と metadata template を置きます。

主な file:

- `workload-catalog.json`
- `context-template.json`

benchmark code がまだなくても、reproducible な workload intent を先に固定します。
